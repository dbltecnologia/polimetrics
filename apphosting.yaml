# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
# Secrets são gerenciados pelo Google Secret Manager.
# Para criar: firebase apphosting:secrets:set NOME_DA_VARIAVEL
env:

  # ============================================================
  # FIREBASE ADMIN SDK
  # ============================================================
  # Service account JSON completo (base64 ou raw JSON)
  - variable: FIREBASE_SERVICE_ACCOUNT_KEY
    secret: FIREBASE_SERVICE_ACCOUNT_KEY
    availability:
      - RUNTIME

  # ============================================================
  # FIREBASE WEB (CLIENT-SIDE)
  # ============================================================
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: NEXT_PUBLIC_FIREBASE_API_KEY
    availability:
      - BUILD
      - RUNTIME

  # ============================================================
  # GOOGLE MAPS
  # ============================================================
  - variable: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
    secret: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
    availability:
      - BUILD
      - RUNTIME

  # Coordenadas padrão do mapa (centro do mapa político)
  - variable: NEXT_PUBLIC_MAP_DEFAULT_LAT
    value: "-15.7942"   # Brasília — ajuste para sua região
    availability:
      - BUILD
      - RUNTIME

  - variable: NEXT_PUBLIC_MAP_DEFAULT_LNG
    value: "-47.8825"   # Brasília — ajuste para sua região
    availability:
      - BUILD
      - RUNTIME

  # ============================================================
  # URL PÚBLICA DO APP (usada em links internos e callbacks)
  # ============================================================
  - variable: NEXT_PUBLIC_API_URL
    value: "https://app.zero1dosvotos.com.br"
    availability:
      - BUILD
      - RUNTIME

  # ============================================================
  # SECRETÁRIO VIRTUAL — IA
  # ============================================================

  # Gemini (Google AI) — provedor principal de geração de texto
  - variable: GEMINI_API_KEY
    secret: GEMINI_API_KEY
    availability:
      - RUNTIME

  # Modelo Gemini a usar (opcional — padrão: gemini-1.5-flash)
  - variable: GEMINI_TEXT_MODEL
    value: "gemini-1.5-flash"
    availability:
      - RUNTIME

  # OpenAI — fallback de geração de texto
  - variable: OPENAI_API_KEY
    secret: OPENAI_API_KEY
    availability:
      - RUNTIME

  # ============================================================
  # CHATWOOT (canal de atendimento — opcional, fallback)
  # Estas variáveis são configuradas como env vars simples no painel Firebase.
  # NÃO usar secret: pois não estão no Secret Manager.
  # ============================================================

  - variable: CHATWOOT_BASE_URL
    value: "CHATWOOT_BASE_URL_PLACEHOLDER"
    availability:
      - RUNTIME

  - variable: CHATWOOT_API_ACCESS_TOKEN
    value: "CHATWOOT_API_ACCESS_TOKEN_PLACEHOLDER"
    availability:
      - RUNTIME

  - variable: CHATWOOT_ACCOUNT_ID
    value: "CHATWOOT_ACCOUNT_ID_PLACEHOLDER"
    availability:
      - RUNTIME

  - variable: CHATWOOT_INBOX_ID
    value: "CHATWOOT_INBOX_ID_PLACEHOLDER"
    availability:
      - RUNTIME

  # ============================================================
  # Z-API (canal WhatsApp principal)
  # ============================================================
  - variable: ZAPI_INSTANCE_ID
    value: "3F0842389FC5B298B5B66A3076259B07"
    availability:
      - RUNTIME

  - variable: ZAPI_TOKEN
    value: "9F04D61967A6EE06600B72FC"
    availability:
      - RUNTIME

  - variable: ZAPI_CLIENT_TOKEN
    secret: ZAPI_CLIENT_TOKEN
    availability:
      - RUNTIME
